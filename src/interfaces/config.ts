export interface EnvConfigType {
    PORT: number;
    APP_NAME: string;
    APP_URL: string;
    MAINTENANCE_MODE: boolean;
    DB_HOST: string;
    DB_USERNAME: string;
    DB_PASSWORD: string;
    DB_NAME: string;
    REDIS_HOST: string;
    REDIS_PORT: number;
}